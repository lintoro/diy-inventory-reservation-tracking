# 問題排查與踩坑日誌 (ISSUES_LOG.md)

本文件用於記錄專案開發過程中遭遇之技術問題、踩坑經驗與修復方案。

---

## 踩坑與修復紀錄

### ISSUE-001：參考資料含有中文字元與營業數據，需嚴格防止洩漏與亂碼
- **問題描述**：
  - Windows 控制台下預設編碼讀取 docx / xlsx 中文易產生亂碼。
  - `/參考資料不要上推/` 包含公司內部物料定價與 ERP 庫存原始檔，若上推遠端 GitHub 儲存庫將造成重大機密外洩。
- **排查與對策**：
  - 於 `.gitignore` 明確加入 `/參考資料不要上推/` 與 `scratch/`。
  - 使用 Python 以 `utf-8` 編碼精確提取 XML 內容至本地暫存區，確保繁體中文無失真解析。
- **狀態**：已解決 (2026-09-22)

---

### ISSUE-002：ERP 原始資料包含小計列、合計列與非 640 倉雜項
- **問題描述**：
  - ERP 匯出的原始 Excel（如 `DIY全庫存.xlsx`）中包含每筆料號後方的小計行（品名為空、庫別為 `小計:`），以及門市展示架、多肉植物等非活動物料。
- **對策**：
  - 清洗模組 `gas/02_ERPCleaner.js` 鎖定庫別代碼 `640`，並過濾掉品號為空、品號或品名包含「小計/合計」之無效列，精確對齊核心物料對照表。
- **狀態**：🟢 已解決並通過單元測試驗證 (2026-09-22)

---

### ISSUE-003：Google Sheets 讀取抽盤流水帳時 Date 物件字串比對失敗
- **問題描述**：
  - 在階段二驗證測試中，`有抽盤品項之生效庫存成功由實盤數 50 覆蓋` 與 `來源標記為 [現場實盤優先]` 判定為 FAIL。
- **根因分析**：
  - Google Sheets 在寫入含有時間格式的字串後，底層儲存格會自動解析轉換為 JavaScript `Date` 物件。
  - 後續以 `getValues()` 讀取時，`row[0]` 為 Date 型別，使用 `String(row[0]).startsWith("2026/09/22")` 會因輸出為 GMT 格式字串而回傳 `false`，導致無法識別今日抽盤。
- **解決方案**：
  - 在 `getEffectiveInventory()` 中加入型別安全檢查，若 `row[0] instanceof Date` 則統一調用 `Utilities.formatDate(val, "Asia/Taipei", "yyyy/MM/dd")` 轉換為純日期字串後比對。
- **狀態**：🟢 已修復並完成雲端同步 (2026-09-22)

---

### ISSUE-004：階段四測試因 5 天後逢週日散客保底 35 份導致折疊籃上限為 0
- **問題描述**：
  - 在階段四驗證測試中，`情境B (5天內正常單): 庫存充足時判定為綠燈，允許接單` 判定為 FAIL。
- **根因分析**：
  - 測試執行日為週二（2026/09/22），5 天後恰逢**週日（2026/09/27）**。
  - 系統嚴格執行假日散客保底 35 份；而折疊籃當前受限於長側板短板總量為 24 套。
  - 24 套小於假日散客保底 35 份，系統計算可供預約上限為 $\max(0, 24 - 35) = 0$ 套！因此預訂 5 套被系統正確判定為「紅燈禁接」，防呆邏輯完全正確，但測試案例選品未考慮此極端短板。
- **解決方案**：
  - 將正常單測試商品調整為庫存充足的「手能生巧（商品 "1"，庫存 675 套）」，扣除假日保底 35 份仍有 640 套，確保單元測試不受特定星期日期浮動影響。
- **狀態**：🟢 測試腳本已修正並推送至雲端 (2026-09-22)

---

### ISSUE-005：瀏覽器多重 Google 帳號 (/u/1/) 導致獨立 Web App 連結存取阻擋
- **問題描述**：
  - 使用者點擊外部 Web App 網址時，Google 顯示「很抱歉，目前無法開啟這個檔案。請檢查網址並再試一次。」（網址帶有 `/u/1/`）。
- **根因分析**：
  - 當瀏覽器同時登入個人 Gmail 與企業/校園帳號時，Google 預設會指派 `/u/0/`、`/u/1/`，不同帳號之間的檔案所有權未共享會造成權限阻擋。
- **解決方案**：
  - 在試算表選單原生支援 **`menu_openSidebar()`（右側操作側邊欄）** 與 **`menu_openDialog()`（全功能視窗）**。
  - 使用者無需離開試算表，直接在試算表畫面右側操作業務速查與主管看板，100% 繞過多帳號衝突與免切換視窗。
- **狀態**：🟢 已實作並推送至雲端 (2026-09-22)

---

### ISSUE-006：側邊欄內嵌呼叫時報「讀取儲存空間時發生伺服器錯誤。錯誤代碼：PERMISSION_DENIED」
- **問題描述**：
  - 在試算表開啟側邊欄時，前端呼叫 `google.script.run.api_getDashboardOverview()` 跳出彈窗：
    `連線錯誤: ScriptError: 很抱歉，讀取儲存空間時發生伺服器錯誤。錯誤代碼：PERMISSION_DENIED。`
- **根因分析**：
  - 原程式碼中 `getSpreadsheet()` 優先使用 `SpreadsheetApp.openById(CONFIG.SPREADSHEET_ID)`。
  - 在多重 Google 帳號登入的 Chrome 環境下，`openById` 會向 Drive API 請求跨檔案讀取，而 Google 伺服器若以瀏覽器中另一非擁有者帳號的身分發起請求，Drive 即會判定 PERMISSION_DENIED。
- **解決方案**：
  - 改造 `getSpreadsheet()`：在容器綁定專案（Container-bound Script）中**強制優先調用 `SpreadsheetApp.getActiveSpreadsheet()`**，直接存取當前試算表記憶體，避免走外部 Drive API。
  - 建議使用者若仍遭遇瀏覽器多帳號混淆，開啟單一帳號的「無痕視窗」即可徹底避開權限誤判。
- **狀態**：🟢 程式碼已修復並推送至雲端 (2026-09-22)

---

### ISSUE-007：Web App 存取時呈現階段一測試 JSON，而非 HTML 介面
- **問題描述**：
  - 開啟 Web App 網址時，畫面直接顯示 `test_Phase1_DBStructure()` 的測試結果 JSON 字串，而非預期的雙視角 HTML 操作介面。
- **根因分析**：
  - Apps Script 專案為全域命名空間，當多個檔案定義相同名稱之頂層函式時，後編譯者會直接覆蓋前者。
  - `gas/99_Tests.js` 底部保留了先前測試用的 `doGet(e)` 函式，且檔名字母排序（`99_...`）晚於 `gas/05_API.js`，導致正式 HTML 模板的 `doGet(e)` 被測試專用的 JSON 輸出覆蓋。
- **解決方案**：
  - 移除 `gas/99_Tests.js` 內多餘的 `doGet(e)`，確保整份專案的 Web App 入口唯一定義於 `gas/05_API.js`。
  - 同步調整 `gas/00_Config.js` 之 `getSpreadsheet()`，在獨立 Web App 環境下能安全 Fallback 至 `openById()`，且精簡 `appsscript.json` 去除不必要之 Drive 授權。
  - 重新部署 Deployment `@5`，URL 恢復正常 HTML 介面渲染。
- **狀態**：🟢 程式碼已修復並更新部署至 @5 (2026-09-22)

---

### ISSUE-008：Web App 瀏覽器分頁標題與網頁內容標題未同步更名
- **問題描述**：
  - 即使在 HTML 內修改 `<title>DIY_物料庫存與預約接單管理系統</title>`，瀏覽器分頁依然顯示舊名稱。
- **根因分析**：
  - Google Apps Script 部署之 Web App 外層由 Google 容器包覆，瀏覽器視窗真正的 Document Title 必須在伺服器端 `doGet(e)` 透過 `HtmlOutput.setTitle(...)` 進行指定，前端純 `<title>` 標籤會被 Google 外層結構覆蓋。
- **解決方案**：
  - 在 `gas/05_API.js` 之 `doGet(e)` 加入 `.setTitle("DIY_物料庫存與預約接單管理系統")`，並同步更新 HTML `<title>` 與內部 UI 導航列文字。
- **狀態**：🟢 已修復並部署至 Version @8 (2026-09-23)

---

### ISSUE-009：庫存高達 675 套之商品誤亮紅燈催促下單
- **問題描述**：
  - 「手能生巧」等庫存極為充足（675 套）的體驗商品，抽屜中依然亮出「🔴 短板缺料！最晚下單：2026/09/08」的警示。
- **根因分析**：
  - 原先在計算木桶短板時，若某物料其可用數恰好等於該商品可做組數的最小值（即使該最小值高達 675 套），短板布林旗標 `isBottleneck` 仍為 `true`，而判斷式未檢視庫存是否充足即一律催單。
- **解決方案**：
  - 在 `buildPartInfo()` 中加入庫存水位閾值判斷：當 `possibleUnits > 50`，一律標示為 `🟢 庫存充足`；只有當 `possibleUnits <= 20` 且為短板瓶頸時，才觸發紅燈催促下單警示。
- **狀態**：🟢 已修正邏輯並通過實測 (2026-09-23)

---

### ISSUE-010：推移速查卡在「正在試算...」停滯無反應
- **問題描述**：
  - 庫管點擊「📅 未來 45 天指定日期庫存推移速查」選取日期後，卡片持續顯示「正在試算...」，未呈現推移報表。
- **根因分析**：
  - `api_getProjectionByDate` 原先試圖以字串直接搜尋 `04_動態推移預估` 底表儲存格，因儲存格日期可能包含格式化物件或列數尚未完全展開，造成例外中斷。
- **解決方案**：
  - 重構 `api_getProjectionByDate(targetDateStr)`，採用伺服器記憶體模型即時運算：取得當前有效庫存，加總指定日期前之在途採購，扣除指定日期前之預約占用與該日散客保底（平日10/假日20），即時精確返回，不再受限於試算表儲存格。
- **狀態**：🟢 已修復秒回並部署至 Version @8 (2026-09-23)

---

### ISSUE-011：業務點選活動日右側方案組數未連動，且超過 45 天遠期預約誤觸短板缺料警示
- **問題描述**：
  - 業務在左側「預計活動日期」選取未來的日期（如 2~3 個月後），右側卡片大字組數仍停留在今日靜態在庫量；且超過 45 天之遠期預約因庫存不足誤亮黃燈，引起業務疑慮。
- **根因分析**：
  - 原先 `renderCapacitiesDrawers` 只繪製當前靜態在庫，未與 `bk-date` 的變更事件連動；而 `checkBookingEligibility` 原先只以 15 天為邊界，未將交期極為充裕的 45 天以上情境單獨放行。
- **解決方案**：
  - 建立 `handleBookingDateChanged()`，在日期變更時透過 `api_getProjectionByDate` 即時重繪右側卡片，顯示該活動日扣除散客保底與既有預訂後之預計可用套數。
  - 當日期距今 $> 45$ 天時，後端強制標記 `isOver45Days: true`，右側卡片大字直接顯示「皆可預訂」，左側燈號判定為綠燈正常接單，提示工廠備料交期充裕、排單無限制。
- **狀態**：🟢 已修復並部署至 Version @11 (2026-09-23)

---

### ISSUE-012：方案卡片硬編碼「6 大體驗方案」限制動態自訂商品擴充
- **問題描述**：
  - 畫面標題寫死為「6 大體驗方案可做組數與用料抽屜」，當後台管理員建立第 7 項或刪除商品時，標題與實際方案數量產生矛盾。
- **根因分析**：
  - 前端 HTML 靜態硬編碼「6 大」文字。
- **解決方案**：
  - 全面將業務端與庫管端標題改為「📊 體驗方案可用組數與用料抽屜」及「📦 各體驗方案組數狀況與專屬材料抽屜」，並在標題旁動態附加選定日期之視角標籤（例如 `【2026/12/10 試算視角】`）。
- **狀態**：🟢 已修正並部署至 Version @11 (2026-09-23)

---

### ISSUE-013：用料抽屜未呈現 ERP 帳面數與現場盤點實數對照
- **問題描述**：
  - 用料抽屜表格原先僅顯示「當前在庫數」，同仁無法確認該數字究係來自 ERP 匯入還是現場盤點，透明度不足。
- **根因分析**：
  - 後端 `partsDetail` 未向下傳遞 `erpQty` 與 `cycleCountQty`，前端表格亦缺乏對應欄位排版。
- **解決方案**：
  - 擴充 `calculateProductCapacities` 之 `getCategoryStats` 收集 ERP 640 倉帳面數與今日現場實盤數。
  - 抽屜表格升級為 7 欄式：展開【系統數字 (ERP 640倉)】、【現場盤點實數 (標註實盤採信或未更動)】與【計算生效在庫】，並在表格下方加註雙軌融合規則：「組數計算優先以現場盤點實數為主；未更動則帶入系統數字」。
- **狀態**：🟢 已實作完成並通過測試驗證 (2026-09-23)


